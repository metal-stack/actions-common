---
name: Spell check

on:
  workflow_call:

jobs:
  spell-check:
    name: Run Spell Checker
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check spelling
        uses: crate-ci/typos@v1.45.1
